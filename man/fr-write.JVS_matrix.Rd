% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export.R
\name{fr-write.JVS_matrix}
\alias{fr-write.JVS_matrix}
\title{Exporter un bilan qualité JVS}
\arguments{
\item{x}{un objet \code{\link{JVS_matrix}} à exporter.}

\item{format}{Chaîne de caractère qui défini le format d'output. Les choix
possibles sont \code{"csv"} (par défault) ou \code{"xlsx"}.}

\item{export_dir}{Chemin vers le dossier qui contiendra les exports.}

\item{overwrite}{Booleen. Est ce qu'un fichier existant doit être ré-écrit ?
Par défaut, \code{overwrite = TRUE}.}

\item{...}{autres argument non utilisés}
}
\value{
Renvoie de manière invisible (via \code{invisible(x)}) le même
bilan qualité \code{\link{JVS_matrix}} que \code{x}.
}
\description{
La fonction permet d'écrire le bilan qualité JVS dans un fichier csv ou
Excel.
}
\details{
\itemize{
\item les fichiers xlsx seront exportées avec le package 'openxlsx'.
\item les fichiers csv seront exportées avec le package 'utils'.
}
}
\examples{
# Chemin menant au répertoire contenant le fichier demetra_m et les séries
dir_path <- system.file(
    "extdata", "WS", "WS_world", "Output", "SAProcessing-1",
    package = "JDCruncheR"
)

# Extraire le rapport JVS à partir des fichiers CSV
JVS <- extract_JVS(dir = dir_path)

# Export du rapport JVS dans un fichier Excel
write(JVS, format = "xlsx", export_dir = tempdir(), overwrite = TRUE)

# Export du rapport JVS dans un fichier CSV
write(JVS, format = "csv", export_dir = tempdir(), overwrite = TRUE)

}
\keyword{internal}
