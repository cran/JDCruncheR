% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_JVS.R
\encoding{UTF-8}
\name{extract_JVS}
\alias{extract_JVS}
\title{Extraction of a JVS report}
\usage{
extract_JVS(
  dir = NULL,
  demetra_m = NULL,
  y = NULL,
  sa = NULL,
  s = NULL,
  t = NULL,
  ...
)
}
\arguments{
\item{dir}{path to the directory containing the demetra_m.csv,
series_decomposition_y_cmp.csv, series_decomposition_sa_cmp.csv,
series_decomposition_s_cmp.csv and series_decomposition_t_cmp.csv files.}

\item{demetra_m}{data.frame containing the diagnostics matrix.
If missing or \code{NULL}, the file is searched for in \code{dir}.}

\item{y}{data.frame containing the initial series.
If missing or \code{NULL}, the file is searched for in \code{dir}.}

\item{sa}{data.frame containing the seasonally adjusted series.
If missing or \code{NULL}, the file is searched for in \code{dir}.}

\item{s}{data.frame containing the seasonal component of the series.
If missing or \code{NULL}, the file is searched for in \code{dir}.}

\item{t}{data.frame containing the trend component series.
If missing or \code{NULL}, the file is searched for in \code{dir}.}

\item{...}{Other parameters to pass to \code{read_demetra_m()} such as \code{sep} (the
separator used in the csv file. By default, \code{sep = ";"}) and \code{dec}
(the decimal separator used in the csv file. By default, \code{dec = ","})}
}
\value{
a \code{\link{JVS_matrix}} object.
}
\description{
Extract a JVS report from the output files of JDemetra+.
The output files can be generated with the GUI or with the cruncher and are
CSV files containing the diagnostics matrix and the output series.
}
\details{
This function generates a JVS report from the output of JDemetra+.
The output needed are the demetra_m diagnostics matrix (usually from the file
\emph{demetra_m.csv}) and the series y, sa, s and t (usually read from the
series CSV files).

All this files can be generated by launching the cruncher (functions
\code{\link[rjwsacruncher]{cruncher_and_param}}).

For more information about the generation of the output, see the vignette:
\code{browseVignettes(package = "JDCruncheR")}

If the series are provided, they have to be \code{data.frame} with the dates in
the first column and the values of the series in the other columns.

This function returns a \code{\link{JVS_matrix}} object, which is a
\code{data.frame} containing several indications on the seasonal adjustment
of the series.

If all data frames (\code{demetra_m}, \code{y}, \code{sa}, \code{s} and \code{i}) are supplied,
the \code{dir} argument is ignored. Otherwise, \code{dir} should point to the
directory containing the corresponding CSV files.
}
\examples{
# Path leading to the directory containing the needed files

dir_path <- system.file(
    "extdata",
    "WS/WS_world/Output/SAProcessing-1",
    package = "JDCruncheR"
)

# Extract the JVS report from the directory
JVS <- extract_JVS(dir = dir_path)
}
\seealso{
\link[=fr-extract_JVS]{Traduction française}

Other JVS_matrix functions:
\code{\link[=write.JVS_matrix]{write.JVS_matrix()}}
}
\concept{JVS_matrix functions}
